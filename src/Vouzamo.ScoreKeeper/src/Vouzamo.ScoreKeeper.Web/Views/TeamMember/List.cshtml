@model TeamMembers

@{
    Layout = "_Layout";
}

<div class="row">
    @if (Model.PagedEnumerable.HasPreviousPage)
    {
        <a href="?page=@Model.PagedEnumerable.CurrentPage--" class="btn">Previous</a>
    }
    @if (Model.PagedEnumerable.HasNextPage)
    {
        <a href="?page=@Model.PagedEnumerable.CurrentPage++" class="btn">Next</a>
    }
</div>
<div class="row">
    @foreach (var teamMember in Model.PagedEnumerable.Enumerable)
    {
        <div class="col-md-4">
            <h1>@teamMember.Handicap</h1>
            <a asp-action="Get" asp-route-id="@teamMember.Id" class="btn btn-primary">Edit</a>
        </div>
    }
</div>
<div class="row">
    <a asp-action="Post" asp-route-teamId="@Model.Team.Id" class="btn">Create</a>
</div>